<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:common="using:AiVideoEditor.UI.Common"
             x:Class="AiVideoEditor.UI.Views.Panels.InspectorView">

  <UserControl.Styles>
    <!-- Every numeric field: sign, digits and the decimal separator only (NumericInput). -->
    <Style Selector="NumericUpDown">
      <Setter Property="ParsingNumberStyle" Value="{x:Static common:NumericInput.ParsingStyle}"/>
    </Style>
  </UserControl.Styles>

  <Border Background="#252526" BorderBrush="#1A1A1A" BorderThickness="1,0,0,0">
    <DockPanel>

      <Border DockPanel.Dock="Top" Padding="8" BorderBrush="#1A1A1A" BorderThickness="0,0,0,1">
        <TextBlock Text="INSPECTOR" FontSize="11" FontWeight="Bold" Foreground="#9A9A9A"/>
      </Border>

      <ScrollViewer>
        <StackPanel Margin="10" Spacing="16">

          <!-- Nothing selected -->
          <TextBlock IsVisible="{Binding IsNothingSelected}"
                     Text="Nothing selected. Choose an item in the Media Browser or a clip on the timeline to see its properties."
                     Foreground="#6A6A6A" FontSize="11" TextWrapping="Wrap"/>

          <!-- Media Browser selection (Phase 2) -->
          <StackPanel IsVisible="{Binding IsMediaSelected}" Spacing="16">
            <StackPanel Spacing="6">
              <TextBlock Text="MEDIA" FontSize="10" FontWeight="Bold" Foreground="#7A7A7A"/>

              <StackPanel Spacing="6">
                <StackPanel Spacing="2">
                  <TextBlock Text="File Name" Foreground="#7A7A7A" FontSize="10"/>
                  <TextBlock Text="{Binding MediaFileName}" Foreground="#DDDDDD" TextWrapping="Wrap"/>
                </StackPanel>

                <StackPanel Spacing="2">
                  <TextBlock Text="Type" Foreground="#7A7A7A" FontSize="10"/>
                  <TextBlock Text="{Binding MediaTypeLabel}" Foreground="#DDDDDD"/>
                </StackPanel>

                <StackPanel Spacing="2">
                  <TextBlock Text="Format" Foreground="#7A7A7A" FontSize="10"/>
                  <TextBlock Text="{Binding MediaFormatLabel}" Foreground="#DDDDDD"/>
                </StackPanel>

                <StackPanel Spacing="2">
                  <TextBlock Text="File Size" Foreground="#7A7A7A" FontSize="10"/>
                  <TextBlock Text="{Binding MediaFileSizeDisplay}" Foreground="#DDDDDD"/>
                </StackPanel>

                <StackPanel Spacing="2">
                  <TextBlock Text="Path" Foreground="#7A7A7A" FontSize="10"/>
                  <TextBlock Text="{Binding MediaFilePath}" Foreground="#DDDDDD" FontSize="11" TextWrapping="Wrap"/>
                </StackPanel>
              </StackPanel>
            </StackPanel>

            <Rectangle Height="1" Fill="#3A3A3A"/>

            <StackPanel Spacing="6">
              <TextBlock Text="TECHNICAL INFORMATION" FontSize="10" FontWeight="Bold" Foreground="#7A7A7A"/>

              <TextBlock IsVisible="{Binding IsAnalyzing}"
                         Text="Analyzing…" Foreground="#8A8A8A" FontSize="11"/>

              <TextBlock IsVisible="{Binding HasAnalysisError}"
                         Text="{Binding AnalysisErrorMessage}"
                         Foreground="#D98A8A" FontSize="11" TextWrapping="Wrap"/>

              <ItemsControl IsVisible="{Binding HasTechnicalInfo}" ItemsSource="{Binding TechnicalRows}">
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <StackPanel Spacing="2" Margin="0,0,0,6">
                      <TextBlock Text="{Binding Label}" Foreground="#7A7A7A" FontSize="10"/>
                      <TextBlock Text="{Binding Value}" Foreground="#DDDDDD"/>
                    </StackPanel>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
            </StackPanel>
          </StackPanel>

          <!-- Timeline clip selection (Phase 4) -->
          <StackPanel IsVisible="{Binding IsTimelineClipSelected}" IsEnabled="{Binding IsEditingAllowed}" Spacing="16">
            <!-- Text (Phase 7 Step 8): text clips only. Every field applies live; consecutive changes of one
                 field merge into one undo step. The color applies once it is a complete #RRGGBB. -->
            <StackPanel Spacing="6" IsVisible="{Binding HasTextProperties}">
              <TextBlock Text="TEXT" FontSize="10" FontWeight="Bold" Foreground="#7A7A7A"/>
              <TextBox Text="{Binding TextContent, Mode=TwoWay}" AcceptsReturn="True" TextWrapping="Wrap"
                       MinHeight="60" MaxHeight="160"/>
              <StackPanel Spacing="6">
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <TextBlock Text="Font" Width="80" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                  <ComboBox ItemsSource="{Binding FontFamilies}" SelectedItem="{Binding FontFamilyName, Mode=TwoWay}" Width="180"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <TextBlock Text="Size" Width="80" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                  <NumericUpDown Value="{Binding FontSize, Mode=TwoWay}" Increment="1" Minimum="{Binding MinFontSize}" Maximum="{Binding MaxFontSize}"
                                 FormatString="0.##" Width="110"/>
                  <TextBlock Text="px" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <TextBlock Text="Color" Width="80" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                  <TextBox Text="{Binding TextColorHex, Mode=TwoWay}" Width="110" MaxLength="7"/>
                  <Border Width="22" Height="22" CornerRadius="2" BorderBrush="#5A5A5A" BorderThickness="1"
                          Background="{Binding TextColorSwatch}" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <TextBlock Text="Alignment" Width="80" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                  <ComboBox ItemsSource="{Binding Alignments}" SelectedItem="{Binding Alignment, Mode=TwoWay}" Width="110"/>
                </StackPanel>
              </StackPanel>
            </StackPanel>

            <!-- Transform and opacity (Phase 7): video, image and text clips. Every change is one edit;
                 consecutive changes of one field merge into one undo step. -->
            <StackPanel Spacing="6" IsVisible="{Binding HasVisualProperties}">
              <TextBlock Text="TRANSFORM" FontSize="10" FontWeight="Bold" Foreground="#7A7A7A"/>
              <StackPanel Spacing="6">
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <TextBlock Text="Position X" Width="80" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                  <NumericUpDown Value="{Binding PositionX, Mode=TwoWay}" Increment="1" Minimum="{Binding MinPosition}" Maximum="{Binding MaxPosition}"
                                 FormatString="0.##" Width="110"/>
                  <TextBlock Text="px" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <TextBlock Text="Position Y" Width="80" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                  <NumericUpDown Value="{Binding PositionY, Mode=TwoWay}" Increment="1" Minimum="{Binding MinPosition}" Maximum="{Binding MaxPosition}"
                                 FormatString="0.##" Width="110"/>
                  <TextBlock Text="px" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <TextBlock Text="Scale" Width="80" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                  <NumericUpDown Value="{Binding ScalePercent, Mode=TwoWay}" Increment="1" Minimum="{Binding MinScalePercent}" Maximum="{Binding MaxScalePercent}"
                                 FormatString="0.##" Width="110"/>
                  <TextBlock Text="%" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <TextBlock Text="Rotation" Width="80" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                  <NumericUpDown Value="{Binding Rotation, Mode=TwoWay}" Increment="1" Minimum="{Binding MinRotation}" Maximum="{Binding MaxRotation}"
                                 FormatString="0.##" Width="110"/>
                  <TextBlock Text="°" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <TextBlock Text="Opacity" Width="80" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                  <NumericUpDown Value="{Binding OpacityPercent, Mode=TwoWay}" Increment="1" Minimum="{Binding MinOpacityPercent}" Maximum="{Binding MaxOpacityPercent}"
                                 FormatString="0.#" Width="110"/>
                  <TextBlock Text="%" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                </StackPanel>
              </StackPanel>
            </StackPanel>

            <!-- Crop (Phase 7): pictures only, fractions of the source per edge. -->
            <StackPanel Spacing="6" IsVisible="{Binding HasCrop}">
              <TextBlock Text="CROP" FontSize="10" FontWeight="Bold" Foreground="#7A7A7A"/>
              <StackPanel Spacing="6">
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <TextBlock Text="Left" Width="80" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                  <NumericUpDown Value="{Binding CropLeftPercent, Mode=TwoWay}" Increment="1" Minimum="{Binding MinCropPercent}" Maximum="{Binding MaxCropPercent}"
                                 FormatString="0.#" Width="110"/>
                  <TextBlock Text="%" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <TextBlock Text="Top" Width="80" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                  <NumericUpDown Value="{Binding CropTopPercent, Mode=TwoWay}" Increment="1" Minimum="{Binding MinCropPercent}" Maximum="{Binding MaxCropPercent}"
                                 FormatString="0.#" Width="110"/>
                  <TextBlock Text="%" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <TextBlock Text="Right" Width="80" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                  <NumericUpDown Value="{Binding CropRightPercent, Mode=TwoWay}" Increment="1" Minimum="{Binding MinCropPercent}" Maximum="{Binding MaxCropPercent}"
                                 FormatString="0.#" Width="110"/>
                  <TextBlock Text="%" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <TextBlock Text="Bottom" Width="80" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                  <NumericUpDown Value="{Binding CropBottomPercent, Mode=TwoWay}" Increment="1" Minimum="{Binding MinCropPercent}" Maximum="{Binding MaxCropPercent}"
                                 FormatString="0.#" Width="110"/>
                  <TextBlock Text="%" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                </StackPanel>
              </StackPanel>
            </StackPanel>

            <!-- Speed (Phase 7 Step 9, D022): video and audio clips; 0.25×–4× in steps of 0.05×. The start and
                 the source range stay, the duration follows. Consecutive changes merge into one undo step. -->
            <StackPanel Spacing="6" IsVisible="{Binding HasSpeed}">
              <TextBlock Text="SPEED" FontSize="10" FontWeight="Bold" Foreground="#7A7A7A"/>
              <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBlock Text="Speed" Width="80" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                <NumericUpDown Value="{Binding SpeedValue, Mode=TwoWay}" Increment="{Binding SpeedIncrement}"
                               Minimum="{Binding MinSpeed}" Maximum="{Binding MaxSpeed}" FormatString="0.00" Width="110"/>
                <TextBlock Text="×" Foreground="#AAAAAA" VerticalAlignment="Center"/>
              </StackPanel>
            </StackPanel>

            <!-- Audio (Phase 7): volume and mute are independent; every change is one edit. -->
            <StackPanel Spacing="6" IsVisible="{Binding HasAudioProperties}">
              <TextBlock Text="AUDIO" FontSize="10" FontWeight="Bold" Foreground="#7A7A7A"/>

              <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBlock Text="Volume" Width="80" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                <NumericUpDown Value="{Binding VolumePercent, Mode=TwoWay}" Increment="1"
                               Minimum="0" Maximum="200" FormatString="0.#" Width="110"/>
                <TextBlock Text="%" Foreground="#AAAAAA" VerticalAlignment="Center"/>
              </StackPanel>

              <!-- Not focusable: Space stays Play/Pause instead of toggling the box after a click. -->
              <CheckBox Content="Mute" IsChecked="{Binding IsMuted, Mode=TwoWay}" Focusable="False"
                        Margin="88,0,0,0" Foreground="#DDDDDD"/>
            </StackPanel>

            <StackPanel Spacing="6">
              <TextBlock Text="CLIP" FontSize="10" FontWeight="Bold" Foreground="#7A7A7A"/>

              <StackPanel Spacing="6">
                <StackPanel Spacing="2">
                  <TextBlock Text="Source" Foreground="#7A7A7A" FontSize="10"/>
                  <TextBlock Text="{Binding ClipName}" Foreground="#DDDDDD" TextWrapping="Wrap"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Spacing="8">
                  <TextBlock Text="Type" Width="80" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                  <TextBlock Text="{Binding ClipTypeLabel}" Foreground="#DDDDDD" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Spacing="8">
                  <TextBlock Text="Start Time" Width="80" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                  <TextBlock Text="{Binding StartTimeDisplay}" Foreground="#DDDDDD" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Spacing="8">
                  <TextBlock Text="End Time" Width="80" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                  <TextBlock Text="{Binding EndTimeDisplay}" Foreground="#DDDDDD" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Spacing="8">
                  <TextBlock Text="Duration" Width="80" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                  <TextBlock Text="{Binding DurationDisplay}" Foreground="#DDDDDD" VerticalAlignment="Center"/>
                </StackPanel>

                <TextBlock Text="{Binding ClipFrameRateDisplay, StringFormat='Timecode (non-drop-frame) at {0}'}"
                           Foreground="#6A6A6A" FontSize="10" TextWrapping="Wrap"/>
              </StackPanel>
            </StackPanel>
          </StackPanel>

        </StackPanel>
      </ScrollViewer>

    </DockPanel>
  </Border>

</UserControl>
